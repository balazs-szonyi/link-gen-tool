'use strict';

// content.js consumes this queue after constructing the UI. Starting at one
// makes the otherwise hidden in-page panel visible immediately in this window.
globalThis.__lgtToggleQueue = 1;
